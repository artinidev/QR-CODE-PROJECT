# MongoDB Connection
# Replace with your local or Atlas URI
MONGODB_URI=mongodb://localhost:27017/pdi-platform

# JWT Secret for authentication
# Generate a random string (e.g., using `openssl rand -base64 32`)
JWT_SECRET=your_jwt_secret_here

# Public URL for the application
# Used for generating invitation links and QR codes
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_URL=http://localhost:3000

# SMTP Configuration for Email Invitations
# Required for Invite User flow
# Options: gmail, sendgrid, outlook, or ethereal (for testing)

# Example for Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Example for Ethereal (Development):
# Auto-generated by setup scripts usually
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test_user@ethereal.email
SMTP_PASS=test_pass
